# Agent-Ready Skills

This directory contains `673` compact skill entrypoints, grouped by category and source tier. Each entrypoint is a separate `SKILL.md` file with frontmatter and a local pointer to the audited source mirror under `included/skills/`.

Use these files when an agent needs a concise starting point. Use the source mirrors when full upstream detail is required.
