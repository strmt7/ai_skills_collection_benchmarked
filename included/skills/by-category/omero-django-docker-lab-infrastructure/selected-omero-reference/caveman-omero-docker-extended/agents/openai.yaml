interface:
  display_name: "caveman"
  short_description: "Opt-in terse mode for all AI agents' internal replies only; reduces tokens while keeping repository prose normal"
  brand_color: "#6B7280"
  default_prompt: "Use caveman mode only when the user explicitly asks for lower-token replies; keep this on-demand across agents and keep docs, comments, docstrings, function descriptions, and user-facing text in normal prose"
policy:
  allow_implicit_invocation: false
